program: train.py
method: grid
# Test LoRA rank - alpha is auto-computed as 2*r
parameters:
  lora_r:
    values: [4, 8, 16, 32, 64]
command:
  - ${env}
  - uv
  - run
  - accelerate
  - launch
  - ${program}
  - "@configs/offpolicy.toml"
  - --no-resume
  - ${args}
